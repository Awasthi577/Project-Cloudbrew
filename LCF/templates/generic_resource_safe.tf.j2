{# ===============================================================
   PYTHON-BACKED GENERIC RESOURCE
   Logic delegated to OpenTofuAdapter._render_hcl_field_python
   =============================================================== #}
resource "{{ resource_type }}" "{{ name }}" {
    {% for key, value in spec.items() %}
        {% if key not in ["type", "provider"] %}
{{ render_field(key, value) }}
        {% endif %}
    {% endfor %}
}
